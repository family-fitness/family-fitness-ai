# AI-8 · 임베딩 · 색인 · 검색

청크를 벡터로 바꾸고, 색인하고, 연령 필터가 걸린 검색을 낸다. 규약은 `../04` §2·§3·§5.

**저장 위치는 `vector_store` 가 아니라 백엔드의 `ai_documents` 다.**
`../01` §4.1 의 스키마는 우리가 제안한 것이고, 실제로 서는 것은
`family-fitness-be` 의 `docs/erd.dbml` 이다 —
대조는 [AI-13](AI-13-backend-contract-reconciliation.md) §3.3·§3.4.

---

## 1. 저장소는 우리 것이다

**백엔드 ERD에서 `ai_documents` 표가 없어졌다** (19표 → 18표). 벡터 저장소를 우리가
온전히 소유한다 ([AI-13](AI-13-backend-contract-reconciliation.md) §3.3).

| 항목 | 상태 |
|---|---|
| 스키마 | **우리가 정한다** — `../01` §4.1 의 `vector_store` |
| 임베딩 모델·차원 | **우리가 고른다.** 백엔드 문서에 언급이 없다 |
| 인용 전달 | `coach_message_citations.chunk_id varchar(200)` 에 우리 문자열이 그대로 담긴다 |
| 백엔드 접근 | 없다. 아무도 우리 행을 건드리지 않는다 |

**이 갈래를 막던 둘이 문제 자체로 사라졌다.** 연령 상한 컬럼과 결정적 `chunk_id` 를
백엔드에 요청할 이유가 없어졌다 — 둘 다 우리 스키마의 일이다.

### 1.1 대신 두 규약이 홀로 선다

기댈 외부 제약이 없다는 뜻이기도 하다.

- **연령 안전** (`../04` §3) — `WHERE` 절에 필터가 있어야 한다. 백업이 없다
- **`chunk_id` 결정성** (`../04` §1) — 재색인 때 같은 원문이 같은 id 를 갖는 것을
  강제할 제약이 없다. **색인 코드가 그 불변식을 검사한다** (§3.1)

### 1.2 FAISS 는 폴백이 아니라 로컬 개발용이다

`../01` §1 은 "DB가 서기 전에도 개발이 멈추지 않게" FAISS를 두었다. 저장소가 우리
것이 됐으므로 배포 기본값은 `pgvector` 이고 FAISS는 로컬 전용이다. `VECTOR_BACKEND`
는 남긴다 — 호출부가 같아야 한다는 이유는 그대로다.

### 1.3 임베딩 모델 — `bge-m3` · 1024차원 (2026-09-16 결정)

| | |
|---|---|
| 모델 | `BAAI/bge-m3` (GGUF `Q8_0` · 0.63GB) |
| 차원 | **1024** — `../04` §6 ① 이 전제한 값 그대로다 |
| 어디서 | 로컬 `llama serve --embedding`. 외부 호출도 키도 없다 |

**다국어 검색에 가장 널리 쓰이는 기본 선택이다** (`AGENTS.md` §7). 코퍼스가 2천 청크
수준이라 더 큰 모델을 고를 이유가 없고, 한국어 질의와 한국어 청크를 같은 공간에 둔다.

**개발과 배포가 같은 모델을 쓴다.** 모델을 바꾸면 전량 재색인이고 `SIM_THRESHOLD` 도
다시 재야 한다 (`../04` §5 · §5). 문구 생성 LLM 과 달리 로컬·배포를 가르지 않는 이유다.
호출부는 바꿔 끼울 수 있게 두되, 바꾸는 순간 재색인이 따라온다는 것을 문서로 못박는다.

---

## 2. 청크를 모은다

| `source` | 어디서 | 상태 |
|---|---|---|
| `prescription` | [AI-6](AI-6-prescription-corpus.md) | 이 갈래에서 만든다 |
| `video` | [AI-7](AI-7-video-labeling.md) | 이 갈래에서 만든다 |
| `criteria` | `data/release/grade_thresholds.csv` | **이미 있다.** 여기서 청크로 편다 |
| `safety` | — | **만들지 않는다** (2026-09-14 결정 · `../04` §1) — 경고 문구는 프론트·백엔드가 띄운다 |
| `facility` | 포함 여부 미정 (`../04` §6 ③) | 넣지 않는다 |

**우리 `source` 5종과 `ai_documents.document_kind` 의 값 집합을 맞춰야 한다.**
ERD 주석의 예시는 `GUIDE`·`VIDEO_CAPTION` 이다. 어느 쪽 어휘를 쓸지는
[AI-13](AI-13-backend-contract-reconciliation.md) 의 합의에 붙인다.

**`criteria` 청크는 연령 무관이다** (`../04` §2.2). 청크의 연령 값은 "이 자료를
누구에게 보여도 되는가"이지 "누구에 관한 자료인가"가 아니다.

---

## 3. 색인

```bash
source .venv/bin/activate                                       # AGENTS.md §5
llama serve -hf gpustack/bge-m3-GGUF -hff bge-m3-Q8_0.gguf --embedding --port 8082  # §1.3
python -m family_fitness_ai.rag.chunks                          # 세 소스를 한 파일로 (§2)
python -m family_fitness_ai.rag.index --corpus-version 2026-09-16.1
```

배치 태스크만 색인한다. **온라인 요청 처리 중에 코퍼스가 바뀌지 않는다**
(`../01` §4.2 · `../04` §5).

### 3.1 색인 전 검사 — 통과하지 못하면 색인하지 않는다

| 검사 | 왜 |
|---|---|
| `prescription`·`video` 청크의 연령이 비어 있지 않다 | `../04` §2.2. 이 검사가 "연령 무관"을 안전하게 만드는 짝이다 |
| `natural_key` 가 내용에서만 나왔다 | `../04` §1. 순번·수집 시각을 쓰지 않는다 |
| 같은 내용이 같은 행으로 upsert된다 | §2.2 |
| 청크 길이가 50~400토큰 | `../04` §2.1 |

**빠뜨린 것을 세어 보고한다.** 조용히 거르면 코퍼스가 왜 작은지 알 수 없다.

### 3.2 재색인

`../04` §5 의 표를 따른다. **원자료가 같으면 같은 행을 갱신한다.**

**청킹 규칙을 바꿀 때는 저장된 인용의 대응이 깨지는지 먼저 확인한다.** 사라진
식별자가 있으면 그것을 참조하는 `coach_message_citations.ai_document_id` 가 끊긴다.

`corpus_version` 과 `embedding_version` 은 다른 것이다 — 앞은 코퍼스를 다시 만든
횟수고 뒤는 임베딩 모델의 판이다. `ai_documents` 에는 뒤만 있다.

---

## 4. 검색

`../04` §3 의 표를 그대로 옮긴다.

| 항목 | 값 |
|---|---|
| 1차 top-k | 10 |
| 최종 컨텍스트 | 최대 5청크 |
| 임계값 | `SIM_THRESHOLD` |
| 연령 필터 | **항상.** 상·하한 둘 다 (§2.1이 풀린 뒤) |
| 공간·소음·기구 | **받는다** — [AI-9](AI-9-videos-search.md) §1 |

**연령 필터를 끄는 인자를 만들지 않는다.** 인자가 있으면 언젠가 꺼진다.

**임계값은 `WHERE` 절에 있고 정렬 뒤의 후처리가 아니다** (`../04` §3). 후처리로
거르면 "0건인데 LLM을 호출하는" 경로가 생긴다. pgvector에는 `WHERE` 가 있고
FAISS에는 없으므로, **검색 인터페이스가 (결과, 제외 사유별 건수)를 함께 돌려주게
해서 두 백엔드가 같은 모양을 내게 한다.** `../03` §4.2 의 `filtered_out` 이 이 값이다.

---

## 5. `SIM_THRESHOLD` = **0.50** (2026-09-16 실측)

`../04` §6 ② 가 미결이었고 **기본값이 없다** — 값이 없으면 `/readyz` 가 실패해 서비스가
요청을 받지 않는다 ([AI-3](AI-3-service-skeleton.md) §2.1).

**임의의 숫자를 하드코딩하지 않는다** (`../04` §3). 근거 없는 임계값은 조용히 관련
자료를 버린다. 그래서 쟀다.

```bash
python -m family_fitness_ai.rag.threshold        # 임베딩 서버를 띄운 채 (§3)
```

| 산출 | 내용 |
|---|---|
| `data/release/sim_queries.csv` | 질의 36개 — 처방 16 · 기준 8 · 영상 8 · **무관 4**. 질의마다 찾아야 할 청크를 미리 적는다 |
| `data/interim/sim_eval.csv` | 질의 × 후보 청크 × 점수 × 정답 여부 |

**질의 세트는 커밋한다.** 임베딩 모델이 바뀌면 임계값을 다시 재야 하고, 그때 같은
질의로 재야 비교가 된다.

### 5.1 두 분포 사이가 비어 있다

| | 값 |
|---|---|
| 무관 질의 최고 점수 | **0.483** (`중학교 수학 숙제 도와줘`). 나머지 셋은 0.38~0.42 |
| 정답 최저 점수 | **0.524** |

**그 사이에 아무것도 없다.** 0.49~0.52 어디를 잡아도 재현율 0.852 · 무관 통과 0건으로
같다. 가운데인 **0.50** 을 골랐다 — 양쪽에 0.02 씩 여유가 남는다.

| 임계값 | 재현율 | 무관 통과 |
|---|---|---|
| 0.48 | 0.852 | 1 |
| **0.50** | **0.852** | **0** |
| 0.53 | 0.815 | 0 |

**정밀도(0.031)는 이 결정의 근거가 아니다.** 정답을 나이·성별 칸 하나로 좁게 적었는데
실제로는 이웃 칸이 함께 올라오기 때문이다 — `마흔한 살 남자 운동 추천` 에 `성인-50`·
`성인-59`·`성인-60` 처방이 0.66 대로 붙는다. 나쁜 검색이 아니라 좁은 정답이다.
그래서 **재현율과 무관 질의 누출 0건**으로 골랐다.

### 5.2 후보에도 못 드는 정답이 8개 있다 — 처방만

`prescription:유아기-60-F-본운동`, `prescription:성인-41-M-본운동`,
`prescription:어르신-70-F-본운동` 등 8개가 top-50 안에도 들지 못한다. 임계값을 낮춰도
못 찾는다. 0.50 에서 종류별 재현율은 이렇다.

| 종류 | 재현율 |
|---|---|
| 기준 | 1.000 (16/16) |
| 영상 | 1.000 (8/8) |
| 처방 | 0.733 (22/30) |

**처방 청크는 서로 너무 닮았다.** 같은 연령대의 나이 칸들이 운동 이름 몇 개만 다른
채 같은 문장 틀을 쓰니, 질의의 나이를 임베딩이 구별하지 못한다. 문장 유사도로 풀 일이
아니다.

**이후 적용** — 나이·성별은 검색이 아니라 필터로 좁힌다. 처방 청크에 나이 칸을
메타데이터로 달고 연령 필터와 같은 자리(`WHERE` 절 · §4)에서 거른다. [AI-10](AI-10-coach-messages.md)
이 나이를 아는 채로 질문을 받으므로 그때 붙인다.

---

## 6. 검사

| 무엇 | 왜 |
|---|---|
| 아이 연령대 질의 결과에 성인 전용 청크가 0건 | **안전 문제다** (`../04` §3). 임계가 아니라 절대값 |
| 연령이 빈 `prescription` 청크가 색인되지 않는다 | §3.1 |
| 같은 원문을 두 번 색인하면 행이 하나다 | §2.2 |
| 두 백엔드가 같은 질의에 같은 `filtered_out` 을 낸다 | §5 |
| 임계 미달 청크가 컨텍스트에 없다 | `../04` §3 |
| 임베딩 차원이 1024다 | §1.3 |

---

## 7. 미결

| # | 항목 | 상태 | 확정 전 기본값 |
|---|---|---|---|
| ⑥ | `facility` 포함 여부 | `../04` §6 ③ | 넣지 않는다 — 코퍼스가 작을 때 무관 청크를 늘리는 쪽이 위험하다 |
