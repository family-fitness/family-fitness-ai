# 값을 채운 .env 는 커밋하지 않는다. 여기에는 이름만 둔다.
# 각 변수의 용도는 docs/01-기술-스택.md §3.2.

# 비밀값 — 배포 환경에서는 Secrets Manager
DATABASE_URL=
ANTHROPIC_API_KEY=
YOUTUBE_API_KEY=
KSPO_SERVICE_KEY=
KSPO_ENDPOINT=

# 설정 — 배포 환경에서는 SSM Parameter Store
MLFLOW_TRACKING_URI=
VECTOR_BACKEND=faiss
SIM_THRESHOLD=
