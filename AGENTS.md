# AGENTS.md — 우리가족 체력키움 AI 파트

에이전트가 이 저장소에서 일할 때 읽는다. **규칙의 정본은 `docs/` 다.** 이 문서는
어디를 봐야 하는지와, 문서에 없는 작업 방식만 적는다.

---

## 1. 무엇을 어디서 보나

| 알고 싶은 것 | 문서 |
|---|---|
| 무엇으로 만드나 · 어디에 배포하나 · 무엇을 쓰고 읽나 | `docs/01-기술-스택.md` |
| 데이터를 어디서 받고 어디까지 쓰나 · 점수는 어떻게 내나 | `docs/02-공공데이터-관리-규약.md` |
| 웹서버와 무엇을 주고받나 · 필드는 무엇인가 | `docs/03-인터페이스-명세.md` |
| 코치가 무엇을 근거로 답하나 | `docs/04-RAG-코퍼스-규약.md` |
| 브랜치 · 커밋 · PR | `CONTRIBUTING.md` |
| 기능 하나의 실행 방법·산출물·한계 | `docs/dev/<이슈ID>-<이름>.md` |
| 구현 순서 · 무엇이 무엇을 막고 있나 | `docs/dev/README.md` |
| 백엔드와 어긋나는 곳 | `docs/dev/AI-13-backend-contract-reconciliation.md` |

**코드 구역은 문서와 1:1이다.** `src/family_fitness_ai/` 각 패키지의 `__init__.py`에
그 구역의 규약이 어느 절인지 적혀 있다.

## 2. 역할 경계

AI 파트만 건드린다. **API 서버(Spring Boot)와 프론트(React Native) 코드는 다른
팀원 것이다.** 그쪽에 필요한 변경이 보이면 고치지 말고 알린다. 차이는 AI-13 에 적는다.

**백엔드 구현을 따른다.** AI 를 실제로 부르는 것은 백엔드 코드다. `docs/03` 과 백엔드가
다르면 백엔드가 보내고 읽는 모양에 맞추고 차이를 알린다. 백엔드 저장소는 **읽기만**
한다 — 수정·삭제·git 명령을 쓰지 않는다.

**`docs/03` 은 팀이 합의한 명세다.** 임의로 고치지 않는다.

## 3. 멈추고 물어볼 것

**각 문서 끝의 「미결」 표에 걸린 항목을 임의로 확정하지 않는다.** 그 표는 아직
답이 없다는 기록이지 기본값을 쓰라는 허가가 아니다. 기본값으로 진행하되, 그 선택에
의존하는 코드를 쓰기 전에 알린다.

그 밖에 멈출 것 — 계약(`docs/03`)의 필드를 바꿔야 할 때, 원자료를 새로 받아야 할 때,
외부 API 호출량이 늘어나는 변경.

## 4. 하지 않는 것

- **비밀값을 코드나 커밋에 넣지 않는다.** 이름만 `.env.example`에 추가한다
- **원자료를 커밋하지 않는다.** 목록은 `data/manifest.csv` (`docs/02` §3.1)
- **데이터가 없을 때 더미로 채워 "돌아가게" 만들지 않는다.** 없으면 없다고 보고한다
- **테스트가 실패하면 단정문이 아니라 구현을 고친다**
- **미결 표의 번호를 재배열하지 않는다.** 해소된 항목은 행만 지우고 번호는 비워 둔다 —
  다른 문서가 번호로 참조하고, 갈래를 합칠 때 그 어긋남은 자동으로 잡히지 않는다
- 문서와 코드가 어긋나면 **문서를 먼저 고치고** 영향 범위를 알린다

## 5. 명령

처음 한 번

```bash
python3 -m venv .venv                  # 3.11 이상 (`pyproject.toml`)
source .venv/bin/activate              # fish 는 activate.fish · 윈도우는 Scripts\activate
pip install -e ".[dev]"
pre-commit install                     # 커밋 훅 (ruff)
git config core.hooksPath .githooks    # 브랜치·커밋 메시지 검사
```

**아래의 모든 명령과 `docs/dev` 의 실행 예시는 venv 를 켠 상태를 전제한다.**
새 셸마다 다시 켠다 — `make` 도 그 셸의 `python` 을 쓴다.

```bash
source .venv/bin/activate
```

품질 검사 — `make verify` 가 CI 와 같은 것을 돈다

```bash
make lint    # ruff check + format --check
make types   # mypy
make test    # pytest
make verify
```

데이터 산출 — 원자료 zip 을 푼 뒤. 기준표는 레포에 있어 인자가 필요 없다

```bash
make distribution DATA_DIR=~/받은자료/kspo-measure
python -m family_fitness_ai.rag.prescription --data-dir ~/받은자료/kspo-measure   # 처방 어휘·청크
```

## 6. 현재 상태

진행 상태의 정본은 `docs/dev/README.md` §1 이다. 코드를 새로 넣을 때는 그 구역의
문서(§1)를 먼저 읽고, 문서에 없는 규칙을 코드로 만들지 않는다.

## 7. 작업 방식

- **흔한 방식을 먼저 쓴다.** 새 판정·규칙·지표를 만들기 전에 널리 쓰이는 방법(가장
  가까운 이웃, 인기순 등)으로 되는지 먼저 잰다. 복잡한 쪽은 그보다 낫다는 측정이 있을
  때만 쓴다
- **LLM 에게 판단을 맡기지 않는다.** 무엇을 추천할지는 코드가 데이터로 정하고, LLM 은
  문구만 쓴다
- **재지 않은 것을 사실로 쓰지 않는다.** 수치에는 실행한 스크립트나 산출물이 있어야 한다
- **"처방"은 운동·영상 추천을 부르는 말이다.** 의료·법률 표현으로 다루지 않는다
- **문서는 짧게 쓴다.** `docs/dev` 에 변경 내역·검토 내역 절을 만들지 않는다. 나중에
  할 일은 그 내용이 속한 절 안에 "이후 적용"으로 적는다
