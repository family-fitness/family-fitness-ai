# AGENTS.md — 우리가족 체력키움 AI 파트

에이전트가 이 저장소에서 일할 때 읽는다. **계약의 정본은 `docs/인터페이스-명세.md`**
이고, 돌리는 법은 `README.md`, 브랜치·커밋 규약은 `CONTRIBUTING.md` 다. 이 문서는
그 셋에 없는 것 — 어떤 판단으로 이렇게 짜였는지와, 손댈 때 걸리는 것 — 을 적는다.

---

## 1. 범위

**AI 파트만 건드린다.** API 서버(Spring Boot)와 프론트(React Native)는 다른 팀원
것이다. 그쪽에 필요한 변경이 보이면 고치지 말고 알린다.

내는 것은 HTTP 다섯 자리뿐이다. 승인·저장·알림은 전부 호출자가 한다.
**AI 는 DB 에 쓰지 않는다.**

## 2. 층과 책임

```
api/     FastAPI. 요청 검증과 오류 모양. 판단은 여기서 하지 않는다
coach/   편성(compose) · 질문 답(answer) · 검증(verify) · 실행 보관(runs)
video/   클립 끊기(clips) · 이름 붙이기(labels) · 목록과 한 회 짜기(catalog)
rag/     임베딩 · 인덱스 · 검색 · 의료 질의 가리기
stats/   원자료 → 표(build) · 표 읽기(tables) · 평가(assess)
common/  설정 · 오류 · 측정 항목 표 · 화면 문구 · LLM 호출
```

`common/items.py` 가 측정 항목의 **유일한 출처**다. 이름·단위·요인·방향을 다른
곳에 다시 적지 않는다.

## 3. 이 프로젝트의 판단들

고칠 때 이유를 모르고 되돌리기 쉬운 것들이다.

**영상은 통째로 내보내지 않는다.** 한 편에 운동이 스무 개씩 들어 있다. 화면에
뜨는 운동 이름이 바뀌는 지점마다 끊은 **클립**(대개 1분 안쪽)을 골라
`준비운동 → 본운동 → 정리운동` 으로 잇는다.

**없으면 없다고 하지 않고 넓혀서 권하고, 넓혔다고 말한다.** 국민체력100 처방은
나이 한 살 단위라 만 8세 자리가 비어 있다. 거부하면 여덟 살에게 줄 것이 없어서,
나이 → 연령대 → 성별 → 요인 순으로 넓히고 `notices` 에 적는다. 연령 라벨이 다른
영상도 막지 않는다 — 쓸지는 화면 저쪽에서 정한다.

**LLM 이 없어도 돌아야 한다.** 키가 없거나, 붐비거나, 답이 규칙에 어긋나면 규칙
편성이 대신 선다. 그때는 `steps[2].status` 가 `partial` 로 나가 숨기지 않는다.
LLM 이 고를 수 있는 것은 우리가 정해서 넘긴다 — 목록 밖 id 를 지어내면 버린다.

**거부는 오류가 아니다.** `refused: true` 는 HTTP 200 이다. 예외로 만들면 거부가
오류 로그에 섞여 거부율을 잴 수 없다.

**실패는 한 모양이다.** `{"error": {"code", "message"}}`. FastAPI 가 스스로 내는
404·405 와 예상 못 한 예외까지 `api/app.py` 에서 같은 모양으로 감싼다. 서버 안
경로나 스택은 로그에만 남긴다.

**아이가 읽는 화면이다.** `부족`·`미달`·`하위` 같은 말을 쓰지 않는다
(`common/copy.py` 의 `BANNED_WORDS`). `band`·`factor` 같은 코드값은 화면에 나가지
않고, 표시 문구는 `copy`·`disclaimer`·`notice` 로 따로 낸다.

**부상·통증·질환·약물 질의는 검색조차 하지 않고 거부한다.** 우리 자료는 측정·처방
이지 진료 자료가 아니다. 애매하면 거부 쪽으로 기운다.

## 4. 데이터

**서비스는 원자료를 읽지 않는다.** `data/release` 표만 읽는다. 만드는 차례가
있고, 앞 단계가 뒤 단계의 재료다.

```
make tables       원자료 67만 행 → 또래 분포 · 인증 기준 · 등급 분포
make clips        화면 글자(OCR) → 클립 531개
make clip-labels  클립 이름 264개 → 처방 어휘 · 요인 · 단계 · 조건   (LLM 씀)
```

| 무엇 | 어디 | git 에 |
|---|---|---|
| `data/release/*.csv` | 위 명령이 만든다 | **있다** |
| `data/index/` | 코퍼스 4,251줄 (22MB) | **있다** |
| `data/raw/` | 원자료·프레임·OCR (2.2GB) | 없다 |

**인덱스를 만드는 코드는 없다.** 예전 구현에 있었으나 클립 구조로 갈아엎을 때
같이 걷어냈다. 그래서 `data/index` 를 저장소에 넣어 두었다. 다시 만드는 코드를
쓰면 Git LFS 나 릴리스 자산으로 옮길지 다시 판단한다.

**임베딩 모델을 바꾸면 인덱스가 무효다.** `corpus.json` 에 `embed:bge-m3/1024` 로
박혀 있다. 인덱스를 만들 때 쓴 모델과 질의를 임베딩하는 모델이 같아야 한다.

## 5. 손댈 때 걸리는 것

**표를 만드는 코드는 재현돼야 한다.** `set` 을 그대로 훑으면 파이썬 해시 순서에
딸려 가서 같은 입력으로 두 번 돌린 표가 달라진다. 정렬해서 쓰고,
`PYTHONHASHSEED` 를 바꿔 두 번 돌려 확인한다.

**클립 이름 잇기는 층이 나뉘어 있다.** 글자가 같으면 `exact`, 임베딩 0.90 이상이면
`embed`, 그 아래는 LLM 이 후보 중에서 고른다. 0.90 은 재 보고 정한 값이다 — 그
아래에서는 임베딩이 「캐치볼을 해요」에 「낚시를 해요」를 붙이는 식으로 자주 틀린다.
사람이 고친 줄(`source=human`)은 다시 돌려도 지킨다.

**협응력은 연령대마다 다른 시험을 쓴다.** `017` 은 청소년의 초, `044` 는 유소년의
벽패스 횟수다. 컬럼정의서의 단위 표기가 `044` 에서 틀렸다. 신체조성
(`003`·`004`·`018`·`042`)은 요인이 아니라 **3등급 판정의 관문**이다.

**시험은 LLM 을 부르지 않는다.** 부르면 돈이 들고 답이 매번 달라 시험 노릇을
못 한다. LLM 없는 길이 늘 서 있어야 한다는 것도 여기서 지킨다. 클립 시험 자료는
지어내지 말고 실제 OCR 결과를 잘라 쓴다(`tests/fixtures/screen_frames.json`).

**옛 브랜치로 옮기면 `data/index` 가 사라진다.** `develop` 계열에서는 추적되지만
`main` 이나 옛 feature 브랜치에는 없어서, 전환할 때 git 이 지운다.

## 6. 아직 안 고친 것

실제 편성 결과를 재 보고 찾은 것들이다. 고칠 때 이 목록을 갱신한다.

1. **단계 배분이 무너질 때가 있다.** LLM 이 고른 id 가 단계별 몫(`PHASE_SHARE`)
   검사를 안 거친다. 준비운동이 50%를 먹은 적이 있다.
2. **한 주 안에서 운동이 겹친다.** 중복 제거가 하루 단위로만 걸려 있다.
3. **요인 표시가 대상 요인으로 덮인다.** 클립에 요인 라벨이 없으면 대상 요인이
   찍힌다. `exact`·`embed` 로 이은 클립 48개는 요인 칸이 비어 있다.
4. **`chunk_id` 에 `미달` 이 남아 있다.** `label` 은 `참가` 로 바꿔 내보내지만 id 는
   식별자라 그대로다. 뿌리에서 고치려면 코퍼스를 다시 만들어야 한다.

## 7. 멈추고 물어볼 것

- **계약을 바꿀 때.** `docs/인터페이스-명세.md` 는 백엔드·프론트와의 약속이다.
  바꾸면 그쪽 코드가 깨진다.
- **자료를 새로 걷을 때.** 유튜브·공공데이터 호출은 한도와 약관이 걸린다.
- **LLM 을 더 부르는 변경.** 한도가 빠듯하다. 규칙으로 되는 일을 LLM 으로 옮기지
  않는다.
- **`data/index` 를 다시 만드는 일.** 되돌리기 어렵고 시간이 든다.
