# AGENTS.md — 우리가족 체력키움 AI 파트

에이전트가 이 저장소에서 일할 때 읽는다. **규칙의 정본은 `docs/` 다.** 이 문서는
어디를 봐야 하는지와, 문서에 없는 작업 방식만 적는다.

---

## 1. 무엇을 어디서 보나

| 알고 싶은 것 | 문서 |
|---|---|
| 무엇으로 만드나 · 어디에 배포하나 · 무엇을 쓰고 읽나 | `docs/01-기술-스택.md` |
| 데이터를 어디서 받고 어디까지 쓰나 · 점수는 어떻게 내나 | `docs/02-공공데이터-관리-규약.md` |
| 웹서버와 무엇을 주고받나 · 필드는 무엇인가 | `docs/03-인터페이스-명세.md` |
| 코치가 무엇을 근거로 답하나 | `docs/04-RAG-코퍼스-규약.md` |
| 브랜치 · 커밋 · PR | `CONTRIBUTING.md` |

**코드 구역은 문서와 1:1이다.** `src/family_fitness_ai/` 각 패키지의 `__init__.py`에
그 구역의 규약이 어느 절인지 적혀 있다.

## 2. 역할 경계

AI 파트만 건드린다. **API 서버(Spring Boot)와 프론트(React Native) 코드는 다른
팀원 것이다.** 그쪽에 필요한 변경이 보이면 고치지 말고 `docs/03`에 적어 알린다.

## 3. 멈추고 물어볼 것

**각 문서 끝의 「미결」 표에 걸린 항목을 임의로 확정하지 않는다.** 그 표는 아직
답이 없다는 기록이지 기본값을 쓰라는 허가가 아니다. 기본값으로 진행하되, 그 선택에
의존하는 코드를 쓰기 전에 알린다.

그 밖에 멈출 것 — 계약(`docs/03`)의 필드를 바꿔야 할 때, 원자료를 새로 받아야 할 때,
외부 API 호출량이 늘어나는 변경.

## 4. 하지 않는 것

- **비밀값을 코드나 커밋에 넣지 않는다.** 이름만 `.env.example`에 추가한다
- **원자료를 커밋하지 않는다.** 목록은 `data/manifest.csv` (`docs/02` §3.1)
- **데이터가 없을 때 더미로 채워 "돌아가게" 만들지 않는다.** 없으면 없다고 보고한다
- **테스트가 실패하면 단정문이 아니라 구현을 고친다**
- 문서와 코드가 어긋나면 **문서를 먼저 고치고** 영향 범위를 알린다

## 5. 명령

```bash
pip install -e ".[dev]"   # 처음 한 번
pre-commit install        # 커밋 훅 (ruff)
git config core.hooksPath .githooks   # 브랜치·커밋 메시지 검사

make lint    # ruff check + format --check
make types   # mypy
make test    # pytest
make verify  # 위 셋. CI가 도는 것과 같다
```

## 6. 현재 상태

문서와 디렉터리 뼈대만 있고 구현은 아직 없다. 코드를 새로 넣을 때는 그 구역의
문서(§1)를 먼저 읽고, 문서에 없는 규칙을 코드로 만들지 않는다.
